pipeline {
    agent any

    stages {

        stage('Feature Test') {
            steps {
                sh 'javac app.java'
                echo "Testing feature branch..."
            }
        }
    }
}
