pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo "Building DEV application..."
            }
        }

        stage('Test') {
            steps {
                echo "Running DEV tests..."
            }
        }
    }
}
